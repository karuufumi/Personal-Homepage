# Photo albums

Create one folder per album here, then add the album to `src/data/photoAlbums.ts`.

For example, an image at `public/photos/saigon-2026/market.jpg` is referenced as:

```ts
src: "/photos/saigon-2026/market.jpg"
```
